apiVersion: v1
kind: Service
metadata:
  name: {{ include "media-proxy.fullname" . }}
  labels:
    {{- include "media-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "media-proxy.selectorLabels" . | nindent 4 }}
