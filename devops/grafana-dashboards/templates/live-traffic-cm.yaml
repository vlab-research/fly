apiVersion: v1
kind: ConfigMap
metadata:
  name: live-traffic-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  live-traffic.json: |-
{{ .Files.Get "live-traffic.json" | indent 4 }}
